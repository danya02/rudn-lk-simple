.DS_Store
.thumbs.db
node_modules

# Quasar core related directories
.quasar
/dist
/quasar.config.*.temporary.compiled*

# Cordova related directories and files
/src-cordova/node_modules
/src-cordova/platforms
/src-cordova/plugins
/src-cordova/www

# Capacitor related directories and files
/src-capacitor/www
/src-capacitor/node_modules

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln

# local .env files
.env.local*

private/
# Android signing config: contains keystore passwords, keep local
src-cordova/build.json

# Deploy target for the PWA (lk.rudn-lab.ru): a separate clone of
# rudn-lab/lk.rudn-lab.ru, not a submodule. Ignored so it is not recorded here
# as a bare gitlink -- it has its own history and is pushed from inside.
published/

# Reverse-engineering scratch space (the official APK lives here).
tmp/
